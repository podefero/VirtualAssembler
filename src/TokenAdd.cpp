#include "TokenAdd.h"

void TokenAdd::validate(std::map<std::string, unsigned int> &symbol_table,
                        unsigned int &limit) {
  // token add just uses registers, no validation here;
}
